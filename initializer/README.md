# go-initializer

```(bash)
build: go build -o go-init .

usage: ./go-init [--scm <your GitHub/GitLab/other SCM base URL>] <module name>

e.g.: ./go-init --scm github.com/binhdoitsme go-initializer
```
